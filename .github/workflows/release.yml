name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: npm install

      - name: Build service host binary
        shell: pwsh
        run: |
          cd src-tauri
          cargo build --release --bin mavrodpi-svc
          New-Item -ItemType Directory -Force resources | Out-Null
          Copy-Item target/release/mavrodpi-svc.exe resources/mavrodpi-svc.exe

      - name: Build & release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "MavroDPI ${{ github.ref_name }}"
          releaseBody: "Yeni sürüm çıktı. Uygulama açıkken otomatik güncelleme bildirimi alacaksınız."
          releaseDraft: false
          prerelease: false
          includeUpdaterJson: true
